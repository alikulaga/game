//INCOMPELTE
class Weapon extends Sprite{
    constructor({
        position,
        imageSrc,
        scale
    }) {
        super({
            position,
            imageSrc,
            scale
        })
    } 
    update() {
        // this.position.x = player.position.x - 13
        // this.position.y = player.position.y - 38
        // this.draw()
        
    }
}